pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{js,ts,tsx,json}"
      run: pnpm biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

commit-msg:
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit {1}
